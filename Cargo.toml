[package]
name = "robot-rust-birdbrain"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
tokio = { version = "1", features = ["full"] }
microbit = "0.16.0"

[lints.rust]
dead_code = "allow"
unused_variables = "allow"

