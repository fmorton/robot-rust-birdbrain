[package]
name = "birdbrain"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
tokio = { version = "1", features = ["full"] }

[lints.rust]
dead_code = "allow"
unused_variables = "allow"

